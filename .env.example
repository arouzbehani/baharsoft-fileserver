NODE_ENV=development
PORT=3000
FILESERVER_TOKEN_SIGNING_SECRET=
# FILESERVER_TOKEN_ISSUER=baharsoft-fileserver
# FILESERVER_TOKEN_AUDIENCE=baharsoft-fileserver
# FILESERVER_TOKEN_TTL_SECONDS=300
# FILESERVER_ADMIN_SESSION_TTL_SECONDS=28800
# Optional. Use a unique single-segment path in production; defaults to /admin.
# FILESERVER_ADMIN_PATH=/control-change-this-to-a-random-value
# Required in production. Used only for the one-time first administrator setup.
# FILESERVER_ADMIN_BOOTSTRAP_TOKEN=

# Optional. Defaults are relative to the repository root.
# FILESERVER_DATA_ROOT=C:\path\to\fileserver-data
# FILESERVER_STORAGE_ROOT=C:\path\to\fileserver-storage\tenants
# FILESERVER_DB_PATH=C:\path\to\fileserver-data\fileserver.sqlite
# FILESERVER_QUARANTINE_ROOT=C:\path\to\fileserver-data\quarantine
